rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser uses Firebase Authentication only. All study data is served
    // through the verified Express API, whose Admin SDK bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
